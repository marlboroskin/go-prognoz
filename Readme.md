# Weather CLI

Консольный прогноз погоды на Go.

## Запуск:  

# Weather CLI — Консольный прогноз погоды на Go

Консольная утилита для получения прогноза погоды через терминал. Написана на чистом Go с поддержкой флагов, тестов и валидации.

## Возможности:

- Определение местоположения по IP (если город не указан)
- Проверка существования города через внешнее API
- Форматированный вывод погоды через `wttr.in`
- Покрытие юнит-тестами
- Работает без GUI — только терминал

## Установка и запуск:

### Требования
- Go 1.19+

### Запуск из исходников

```bash
git clone https://github.com/ваш-логин/weather-cli.git
cd weather-cli

# Запуск с указанием города
go run main.go --city="Moscow" --format=3

# Запуск с определением по IP
go run main.go --format=1


```bash
go run main.go --city="Moscow" --format=3

Флаги:

--city — город (если не указан — определяется по IP)
--format — формат вывода (0-4)

Тесты:

```bash
go test ./...
```

## Флаги:

| Флаг       | Описание                                      | Значение по умолчанию     |
|------------|-----------------------------------------------|----------------------------|
| `--city`   | Город для поиска (например, `London`)         | пусто (определяется по IP) |
| `--format` | Формат вывода: 0, 1, 2, 3, 4                  | `1`                        |


> **Поддерживаемые форматы:**
> - `0` — кратко
> - `1` — сегодня
> - `2` — сегодня + завтра
> - `3` — три дня
> - `4` — в виде графика

```bash
# Запуск всех тестов
go test ./...

# Запуск тестов конкретного пакета
go test ./geo
go test ./weather



weather-cli/
├── main.go               # Точка входа, обработка флагов
├── geo/                  # Работа с локацией
│   ├── geo.go            # Получение города
│   └── geo_test.go       # Тесты
└── weather/              # Прогноз погоды
    ├── weather.go        # Запрос к wttr.in
    └── weather_test.go   # Тесты
```

### Запуск тестов конкретного пакета

- go test ./geo go test ./weather


```bash
    $ go run main.go --city="Stavropol" --format=3
Stavropol
Stavropol +13°C 🌤  Humidity: 54%
```

⚠️ Внешние API

```bash
countriesnow.space — проверка существования города
ipai.co — определение местоположения
wttr.in — прогноз погоды
```
